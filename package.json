{
  "name": "lambda-image-resizer",
  "version": "1.0.0",
  "description": "AWS Lambda function for dynamic image resizing from S3",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws",
    "lambda",
    "s3",
    "image",
    "resize"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.515.0",
    "sharp": "^0.33.2"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^4.1.0"
  }
}
